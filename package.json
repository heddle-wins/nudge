{
  "name": "nudge",
  "private": true,
  "version": "0.3.0",
  "description": "Privacy-first browser automation by Heddle.",
  "packageManager": "pnpm@9.15.5",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "demo:portal": "python3 -m http.server 4173 --directory demo/public-service-portal",
    "measure:api": "bash scripts/measure-api-latency.sh",
    "measure:extension-bundle": "node scripts/measure-extension-bundle.mjs",
    "fixtures:rasterize": "node scripts/rasterize-visual-fixtures.mjs",
    "fixtures:vision": "node scripts/run-extension-visual-fixtures.mjs"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
